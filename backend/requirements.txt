# SDK-COMPLIANT: Official Google Gen AI SDK (Latest stable)
google-genai==1.51.0

# WebSocket server
websockets==14.2

# Environment management
python-dotenv==1.0.0

# Optional dependencies
aiohttp==3.9.1

# Firebase Authentication (for user token verification in cloud)
firebase-admin==6.5.0

# Optional: Google Cloud Speech-to-Text (only needed if using 'google-cloud-stt' as captions.source)
# Default is Gemini's built-in transcription (no additional dependency required)
# google-cloud-speech==2.21.0