{
  "api": {
    "model": "models/gemini-2.5-flash-native-audio-preview-09-2025"
  },
  "geminiVoice": {
    "enabled": true,
    "voiceName": "Algenib",
    "affectiveDialog": true
  },
  "automaticVAD": {
    "enabled": true,
    "startOfSpeechSensitivity": "START_SENSITIVITY_LOW",
    "endOfSpeechSensitivity": "END_SENSITIVITY_LOW",
    "prefixPaddingMs": 100,
    "silenceDurationMs": 500,
    "_comment": "Automatic Voice Activity Detection. LOW sensitivity reduces false positives (Gemini only supports LOW/HIGH, no MEDIUM). 500ms silence allows natural speech pauses. Gemini handles interruption detection server-side."
  },
  "audio": {
    "outputSampleRate": 24000,
    "inputSampleRate": 16000,
    "responseModalities": ["AUDIO"],
    "mimeTypeInput": "audio/pcm",
    "mimeTypeOutput": "audio/pcm",
    "chunkSizeBytes": 3200,
    "_comment": "Audio format specifications. chunkSizeBytes: 100ms at 16kHz mono 16-bit PCM"
  },
  "timeouts": {
    "sendTimeoutSeconds": 5,
    "setupTimeoutSeconds": 10,
    "_comment": "Operation timeouts. sendTimeoutSeconds: timeout for sending messages to Gemini. setupTimeoutSeconds: timeout for session setup"
  },
  "captions": {
    "enabled": true,
    "_comment": "Uses Gemini's built-in output_audio_transcription feature. Backend always sends transcriptions. Frontend CC toggle controls display (default: OFF)."
  },
  "initialContext": {
    "enabled": true,
    "includeBackstory": false,
    "includeSetList": true,
    "backstoryPath": "whinny_backstory.json",
    "setListPath": "set-list.json",
    "_comment": "Optional: Send backstory/setlist as first message. Backstory disabled since system_instruction already includes persona. SetList enabled to provide detailed song trivia. Live API does NOT support context caching."
  }
}
